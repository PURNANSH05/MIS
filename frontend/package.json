{
  "name": "medical-inventory-frontend-wrapper",
  "private": true,
  "scripts": {
    "start": "npm --prefix ..\\react-app run start",
    "build": "npm --prefix ..\\react-app run build",
    "test": "npm --prefix ..\\react-app run test"
  }
}
